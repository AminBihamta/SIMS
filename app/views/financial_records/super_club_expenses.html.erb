<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cabin:ital,wght@0,400..700;1,400..700&display=swap" rel="stylesheet">
    <%= render 'shared/header', search_url: equipments_path %>
    <%= stylesheet_link_tag 'financials/expenseDashboard', media: 'all', 'data-turbo-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
</head>

<body>

    <div class="budgetDiv">
        <div class="budget">
            <h2 class="pure">Total Expenses</h2>
        </div>
        <div class="budgetNumber">
            <h1 class="total-amount pure"><%= number_to_currency(@total_super_club_expenses, unit: "RM") %></h1>
        </div>
    </div>

    <% if @super_club_expenses.any? %>
    <!-- Super Club Cards Section -->
    <div class="clubsContainerParent">

        <div class="clubsContainer last-disabled">
            <% @super_club_expenses.each do |club_name, total_expenses| %>
            <div class="club" onclick="window.location='<%= all_super_expenses_financial_record_path(Club.find_by(Club_Name: club_name)) %>'" style="cursor: pointer;">
                <div class="clubDetails">
                    <h2><%= club_name %></h2>
                    <p><%= number_to_currency(total_expenses, unit: "RM") %></p>
                </div>
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="28" viewBox="0 0 16 28" fill="none">
                    <path d="M0 3.23375L2.69167 0.5L16 14L2.69167 27.5L0 24.7663L10.6083 14L0 3.23375Z" fill="#CCF2C2" />
                </svg>
            </div>
            <% end %>
        </div>
    </div>
    <% else %>
    <p class="no-expenses">No super club expenses to display.</p>
    <% end %>
</body>

</html>